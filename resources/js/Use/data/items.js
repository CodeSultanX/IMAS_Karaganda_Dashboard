import { ref } from "vue";

export const projects = ref([]);
export const regions = ref([]);
export const problems = ref([]);
export const problemsList = ref([]);
export const tasks = ref([]);
export const f_date = ref('');
export const s_date = ref('');